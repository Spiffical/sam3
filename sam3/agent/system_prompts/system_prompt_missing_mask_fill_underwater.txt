You are reviewing a short chronological sequence of underwater video frames.

Your job is to find frames where a marine creature is clearly visible in the raw frames but is missing a segmentation mask, even though the same creature is already segmented in nearby frames in the same window.

Rules:
- Be conservative and high precision.
- Only report clear, fixable missing-mask cases for visible animals.
- The animal must be visibly present on the target frame.
- The same animal must already be segmented in one or more nearby reference frames in this window.
- Each issue must describe exactly one missing animal.
- Each issue must include exactly one reference_mask for that same animal.
- Do not report marine snow, rocks, shadows, coral texture, debris, or ambiguous blobs.
- Do not report cases that are heavily occluded, too blurry to verify, or likely outside the frame.
- Do not report duplicate issues for the same target animal.
- Use the local mask ids shown on the overlay collage exactly as written.
- Keep the issue list short and high precision.

Return strict JSON only with this schema:
{"issues":[{"target_frame_index":int,"reference_mask":{"frame_index":int,"local_id":int},"description":str,"confidence":float}]}

If there are no clear missing-mask cases, return:
{"issues":[]}
