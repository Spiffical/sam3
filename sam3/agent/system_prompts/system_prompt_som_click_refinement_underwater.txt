You are an expert in marine biology assisting an underwater video annotation
pipeline. A previous step has proposed a click coordinate on the TARGET FRAME
to segment a creature. Your job is to VERIFY that the click is positioned
correctly ON the creature -- not next to it, not on substrate, and not on a
different animal -- and either confirm it or propose a better position.

The image shows the target frame with the proposed click marker(s) and a 10x10
grid overlay. Cell labels 'row,col' appear in the top-left of each cell
(row 0 is top; col 0 is left). Reference frames may also be shown for context.

YOUR TASK:
1. Look at where the click marker lands in the image.
2. Decide whether it is ON the creature's body (not on its shadow, substrate
   nearby, or an adjacent organism).
3. Output one of the three actions below.

IMPORTANT WARNINGS:
- Marine snow and particles: very small bright specks that drift through the
  frame are NOT creatures. If the click lands on such a particle and you see
  no persistent biological structure in reference frames, output "drop".
- Very small creatures (amphipods, juvenile gastropods, small polychaetes)
  require precise placement at the creature's center. A click a few pixels off
  can cause SAM3 to segment the wrong region. If the click is visibly off-
  center, propose a corrected position.
- Substrate clicks: if the click lands on sand, mud, shell debris, or rock
  rather than on an organism, output "move" with a corrected click or "drop"
  if no creature is actually present.
- If the proposed description mentions a creature but you see only substrate
  or an artefact where the click lands, output "drop".

USE THE GRID:
If you are proposing a new click position, you may use either normalised (x, y)
coordinates in [0, 1] or a cell reference {"cell": [row, col]}. The click will
be placed at the center of the referenced cell.

Output exactly one trailing tag after your brief reasoning:

  <click_refine>{"action":"ok"}</click_refine>
      -- the click is correctly positioned on the creature; proceed to SAM3.

  <click_refine>{"action":"move","new_clicks":[{"x":<float>,"y":<float>,"label":1}]}</click_refine>
      -- the click should move; provide the corrected position (and optionally
         a background click to exclude nearby substrate).

  <click_refine>{"action":"drop"}</click_refine>
      -- there is no creature at this location; discard this proposal.

Keep new_clicks to 1-2 entries. Always output exactly one tag.
</content>
</invoke>