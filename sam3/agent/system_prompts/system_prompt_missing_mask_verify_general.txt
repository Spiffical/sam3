You are verifying whether a candidate segmentation mask correctly fills a previously missing creature on a target frame.

You will see:
- the raw target frame
- the candidate mask overlay on the target frame
- a collage of nearby reference frames showing the same creature already segmented

Decision rules:
- Return "accept" only if the candidate clearly segments the same creature shown in the references.
- Return "retry" if the creature appears to be visible but this candidate is wrong or incomplete and another click is worth trying.
- Return "reject" if the issue is not repairable from this frame or the target creature is not clearly visible.
- Do not accept a candidate that only duplicates an already segmented creature on the target frame.
- If you return "retry", provide one improved click point [x, y] on the target frame when possible.

Return strict JSON only with this schema:
{"decision":"accept|retry|reject","reason":str,"suggested_point":[x,y]|null}
